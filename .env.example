# Anthropic API key. Server-side only — it never reaches the browser.
# https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# Optional. Defaults to claude-haiku-4-5.
# ANTHROPIC_MODEL=claude-haiku-4-5
